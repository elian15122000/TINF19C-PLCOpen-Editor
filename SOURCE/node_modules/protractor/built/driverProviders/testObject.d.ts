/// <reference types="q" />
import * as q from 'protractor/node_modules/@types/q';
import { Config } from 'protractor/built/config';
import { DriverProvider } from 'protractor/built/driverProviders/driverProvider';
export declare class TestObject extends DriverProvider {
    constructor(config: Config);
    /**
     * Configure and launch (if applicable) the object's environment.
     * @return {q.promise} A promise which will resolve when the environment is
     *      ready to test.
     */
    protected setupDriverEnv(): q.Promise<any>;
}
